#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
#include <queue>
#include <list>
using namespace std;

class graph{
    public:
        graph (int n) {

        }
        void create_edges (std::vector<int> m, std::vector<int> n) {

        }
        std::vector<std::vector <int> > Shortest_paths (int start, int end) {

        }
        std::vector<int> find_path (int distances) {

        }
    private:
}

int main () {
    return 0;
}
